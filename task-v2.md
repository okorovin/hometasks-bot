# Telegram Bot "Домашний помощник" — PRD + Technical Specification v2

> Обновлено: 2026-02-17. Основано на task.md с учётом ревью и принятых решений.

## 1. Цель проекта

Создать Telegram-бота для личного использования, который помогает управлять домашними задачами:

- Принимает задачи текстом и через пересылку сообщений
- Напоминает о задачах
- Поддерживает повторяющиеся задачи
- Учитывает тихие часы
- Отправляет ежедневный дайджест

Пользователь: один (владелец бота), с возможностью расширения.

---

## 2. Зафиксированные правила

- Таймзона: `Europe/Moscow`
- Тихие часы: `22:00–09:00`
- Просроченные задачи: напоминание каждый день в `10:00` (вычисляется на лету)
- Ежедневный дайджест: `10:00` (вычисляется на лету)
- Повторы: каждые `N дней/недель/месяцев`
- Повтор считается от даты выполнения (from_completion, единственный режим)
- Формат по умолчанию: быстрый Inbox

---

## 3. Основные сценарии использования

### 3.1 Добавление задачи

Источник:
- Текстовое сообщение
- Пересланное сообщение

> Голосовые сообщения — после MVP.

Поведение (единый flow для всех типов ввода, без подтверждения):
1. Бот получает сообщение
2. LLM анализирует текст → извлекает `title`, `due_at`, описание
3. Для пересланного сообщения: оригинальный текст → `notes`, LLM извлекает короткий `title`
4. Создаёт задачу
5. Отправляет карточку задачи с кнопками управления

Если дата не указана → задача попадает в Inbox без срока.
Если дата указана без времени → `due_at` устанавливается на `09:00` (начало дня после тихих часов).

---

### 3.2 Напоминания

- Если есть срок → напоминание в `due_at` (с учётом тихих часов)
- Если просрочено → напоминание каждый день в `10:00` (вычисляется worker-ом на лету)
- Если повторяющаяся → после "Сделано" создаётся новый экземпляр
- Тихие часы: если напоминание попадает в `22:00–09:00` → переносится на `09:05`

---

### 3.3 Ежедневный дайджест (10:00)

Вычисляется worker-ом на лету (не хранится в таблице reminders).

Содержит:
- Просроченные задачи
- Задачи на сегодня
- Количество задач в Inbox

---

## 4. Команды бота

- `/add` — добавить задачу
- `/today` — задачи на сегодня
- `/inbox` — задачи без срока
- `/overdue` — просроченные
- `/week` — ближайшие 7 дней
- `/all` — все активные задачи
- `/settings` — настройки (таймзона, тихие часы, время дайджеста)
- `/help`

---

## 5. Карточка задачи (UX)

Формат сообщения:

**Название задачи**
Срок: дата/время или "—"
Повтор: описание или "нет"

Кнопки:
- ✅ Сделано
- ⏰ Отложить (15м / 1ч / завтра / выбрать) — **сдвигает `due_at`**
- 📅 Поставить срок
- 🔁 Настроить повтор
- ✏️ Редактировать (бот просит отправить новый текст)
- 🗑 Удалить (soft delete, с подтверждением "Точно удалить?")

Карточка редактируется in-place через `card_message_id` (не создаётся новое сообщение).

### Пагинация списков

Telegram ограничивает сообщение ~4096 символов. При большом количестве задач — кнопки «← Назад» / «Далее →».

---

## 6. Повторы

Поддерживаемые типы:

- Каждые N дней
- Каждые N недель
- Каждые N месяцев

Логика (только from_completion):

После нажатия "Сделано":
- текущая задача получает `done_at`
- создаётся новая задача
- новая дата = `done_at + interval`

---

## 7. Архитектура

### 7.1 Стек

- Node.js
- TypeScript
- Telegram framework: **grammY**
- PostgreSQL (managed)
- Prisma ORM
- LLM: provider-agnostic (OpenAI-совместимый интерфейс), дефолт **GPT-4o-mini**
- Логирование: **pino** (stdout, уровни info/warn/error)
- Ошибки: отправляются пользователю в Telegram (прозрачность)

### 7.2 Процесс

Один процесс: бот (long-polling) + планировщик (`setInterval`).

Long-polling и в dev, и в prod.

### 7.3 Деплой

- Backend: Fly.io или Render (один контейнер)
- База данных: Neon или Supabase

---

## 8. Безопасность

- `ALLOWED_TELEGRAM_IDS` в env — список разрешённых Telegram user ID
- Middleware: если `ctx.from.id` не в списке → игнорировать, не отвечать
- Не логировать тело сообщений пользователя в prod (персональные данные)

---

## 8.1 Обработка ошибок — уведомления в Telegram

Все ошибки отправляются пользователю в Telegram (прозрачность работы бота):

| Тип ошибки | Сообщение пользователю |
|---|---|
| LLM недоступен / timeout | ⚠️ Не удалось распознать задачу: [причина]. Сохранено как есть. |
| Ошибка БД | ❌ Ошибка базы данных: [краткое описание] |
| Планировщик не смог отправить напоминание | ⚠️ Не удалось отправить напоминание для: [название задачи] |
| Необработанное исключение | ❌ Внутренняя ошибка: [тип]. Проверь логи. |

Правила:
- Сообщение — короткое, человекочитаемое, без стектрейсов (полные ошибки в логах pino)
- Дедупликация: если одна ошибка повторяется — отправлять раз в 5 минут с счётчиком «(×N)»
- При ошибке LLM — fallback: title = исходный текст, due_at = null

---

## 9. Структура базы данных

### users

- id (PK)
- telegram_user_id (unique)
- timezone (default: "Europe/Moscow")
- quiet_from (default: "22:00")
- quiet_to (default: "09:00")
- digest_time (default: "10:00")
- created_at

---

### tasks

- id (PK)
- user_id (FK → users)
- title
- notes (nullable) — тело пересланного сообщения, доп. заметки
- status (active | done | deleted)
- due_at (nullable, **datetime**) — если дата без времени → 09:00
- created_at
- done_at (nullable)
- source_type (text | forward)
- card_message_id (nullable) — ID сообщения-карточки бота для inline-редактирования

---

### repeat_rules

- id (PK)
- task_id (FK → tasks)
- every_n
- unit (day | week | month)
- active (boolean)

> Поле `anchor` убрано — единственный режим from_completion.

---

### reminders

Только для due-напоминаний (конкретное время, конкретная задача).
Дайджест и overdue вычисляются worker-ом на лету.

- id (PK)
- task_id (FK → tasks)
- remind_at (datetime)
- state (scheduled | sent | cancelled)

---

## 10. Планировщик (scheduler)

Работает в том же процессе через `setInterval` (~60 сек).

Каждый тик:

1. **Due-напоминания**: `SELECT FROM reminders WHERE remind_at <= NOW() AND state = 'scheduled'` → отправить → `state = 'sent'`
2. **Дайджест** (если наступило `digest_time`): собрать overdue + today + inbox count → отправить
3. **Overdue**: `SELECT FROM tasks WHERE due_at < TODAY AND status = 'active'` → отправить напоминание (раз в день)

Тихие часы:
- Если `remind_at` попадает в `22:00–09:00` → переносится на `09:05`

---

## 11. Использование LLM

Провайдер: OpenAI-совместимый интерфейс.
Конфигурация через env: `LLM_BASE_URL`, `LLM_API_KEY`, `LLM_MODEL`.
Дефолт: OpenAI GPT-4o-mini.

LLM применяется для:
- Парсинга текста в структуру задачи (title, due_at)
- Определения даты и повторов из естественного языка
- Человечной формулировки title из длинных пересланных сообщений

LLM НЕ отвечает за:
- Вычисление дат (только извлечение из текста → код считает datetime)
- Планирование напоминаний
- Бизнес-логику повторов

---

## 12. Критерии готовности MVP

- [ ] Создание задачи текстом
- [ ] Создание задачи через пересылку (с notes)
- [ ] Команды /today, /inbox, /overdue, /week, /all
- [ ] Карточка задачи с inline-редактированием
- [ ] Кнопка "Сделано"
- [ ] Кнопка "Отложить" (сдвигает due_at)
- [ ] Кнопка "Редактировать"
- [ ] Кнопка "Удалить" с подтверждением
- [ ] Кнопка "Поставить срок"
- [ ] Повторы N дней/недель/месяцев (from_completion)
- [ ] Кнопка "Настроить повтор"
- [ ] Ежедневный дайджест в 10:00
- [ ] Напоминания по due_at
- [ ] Ежедневные напоминания по просроченным
- [ ] Тихие часы (перенос на 09:05)
- [ ] Пагинация списков
- [ ] Команда /settings
- [ ] Безопасность (whitelist middleware)
- [ ] Логирование (pino)

---

## 13. Следующий этап

После MVP:

- Голосовые сообщения (Whisper STT)
- Расширенные повторы (по дням недели, from_due_date)
- Категории/теги
- Мини-веб интерфейс
- Интеграции (Google Calendar, Notion и т.д.)
- Webhook-режим для prod

---

Проект ориентирован на простоту поддержки, минимальную инфраструктуру и предсказуемую логику работы.
